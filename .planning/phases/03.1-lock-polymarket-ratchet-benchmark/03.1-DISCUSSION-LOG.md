# Phase 03.1 Discussion Log

**Date:** 2026-04-08
**Mode:** Auto-captured from completed implementation context

## Summary

Phase `03.1` was inserted as urgent follow-on work after the Polymarket strategy was materially improved outside the main milestone sequence. The purpose of this discussion log is to preserve the decisions that make the improvement ratchetable before later strategy phases continue.

## Auto-selected decisions

1. Preserve the newly improved BTC Polymarket behavior as the next ratchet floor before broader strategy design resumes.
2. Use the actual historical Polymarket coverage window (`2025-08-08` onward) for the benchmark instead of a misleading longer chart span.
3. Pin the route-level strategy outcome exposed by `/api/chart`, because that is the user-visible contract.
4. Preserve the relevance-weighted signal path and its exposed fields (`signal_skew_ratio`, `signal_source`) rather than reverting to raw aggregate skew semantics.
5. Treat benchmark fixture creation and regression coverage as the main deliverable of this inserted phase, not another redesign of the strategy itself.

## Notes

- The implementation already exists locally in `lib/polymarket.py`, `routes/polymarket.py`, and `tests/test_polymarket.py`.
- The inserted phase is intended to convert that implementation into GSD-tracked benchmark and ratchet artifacts so future work cannot quietly erase the improvement.

---
phase: 03.2-lock-current-strategy-configuration-ratchet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/strategy_config_ratchet.json
  - tests/test_strategy_config_ratchet.py
  - docs/current-strategy-config-ratchet.md
  - .planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md
autonomous: true
requirements:
  - RATCHET-03
  - COMP-03
user_setup: []
must_haves:
  truths:
    - The current backtest configuration surface is frozen in a machine-readable spec covering strategy selector order, money-management defaults, and route strategy inventory.
    - The ratchet uses the same user-visible surfaces the app depends on: `/backtest`, the current JS default strategy, and `/api/chart`.
    - The route-level config test runs deterministically from committed BTC and Polymarket fixtures instead of live data.
  artifacts:
    - tests/fixtures/strategy_config_ratchet.json
    - tests/test_strategy_config_ratchet.py
    - .planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md
  key_links:
    - `tests/fixtures/strategy_config_ratchet.json` is the machine-readable source of truth for the current configuration contract.
    - `tests/test_strategy_config_ratchet.py` proves the backtest template, JS default, and `/api/chart` route still match that contract.
    - `.planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md` records the pinned defaults and strategy inventory in human-readable form.
---

<objective>
Freeze the current strategy configuration surface into a deterministic ratchet so future changes cannot silently alter defaults, option ordering, or backend strategy inventory.

Purpose: this strengthens the milestone ratchet beyond performance-only checks and makes the current backtest product contract explicit before Phase 4 changes continue.
Output: one machine-readable config spec, one focused ratchet test, and matching docs/baseline artifacts.
</objective>

<execution_context>
@/Users/adrianosullivan/projects/trading/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adrianosullivan/projects/trading/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-lock-current-strategy-configuration-ratchet/03.2-CONTEXT.md
@templates/partials/backtest_panel.html
@static/js/backtest_panel.js
@static/js/backtest_report.js
@routes/chart.py
@tests/test_focus_basket_benchmark_backtests.py
@tests/test_polymarket_benchmark_backtests.py
@tests/fixtures/btc_usd_1d_benchmark.csv
@tests/fixtures/polymarket_probability_history_benchmark.json
</context>

<threat_model>
## Trust Boundaries

- The configuration ratchet must use committed fixtures and committed templates/JS files, or the guard will drift with runtime state.
- The strategy selector order and defaults are user-visible product behavior; accidental changes here are integrity-sensitive even if the app still renders.
- The route contract must be checked through `/api/chart`, not through hand-maintained lists, or the UI/backend mapping can drift without detection.

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-03.2-01 | tampering | strategy selector / MM option order | mitigate | Freeze the current UI config in a JSON spec and assert exact option sequences from `/backtest`. |
| TM-03.2-02 | tampering | JS default strategy fallback | mitigate | Assert `BT_DEFAULT_STRATEGY` remains `ribbon` and document intentional update workflow if that baseline changes later. |
| TM-03.2-03 | repudiation | route/backend strategy inventory | mitigate | Patch `/api/chart` against frozen BTC and Polymarket fixtures, then assert the expected strategy keys and required fields from a single committed spec. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the current backtest configuration contract in a machine-readable spec</name>
  <files>tests/fixtures/strategy_config_ratchet.json, docs/current-strategy-config-ratchet.md, .planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md</files>
  <read_first>templates/partials/backtest_panel.html, static/js/backtest_panel.js, static/js/backtest_report.js, routes/chart.py, tests/fixtures/polymarket_benchmark_backtests.json</read_first>
  <action>Create `tests/fixtures/strategy_config_ratchet.json` as the source-of-truth spec for the current backtest config contract. Include top-level keys exactly `backtest_defaults`, `strategy_options`, `money_management`, and `route_contract`. In `backtest_defaults`, pin `default_strategy: "ribbon"`, `ticker: "BTC-USD"`, `interval: "1d"`, `period: "10"`, and `multiplier: "2.5"`. In `strategy_options`, record the exact current selector order from `backtest_panel.html`, including `ribbon`, `corpus_trend`, and `polymarket`. In `money_management`, capture the exact current defaults and option lists for `sizing`, `stop`, `risk_cap`, and `compound`, plus the current stop-input default value `3`. In `route_contract`, include a deterministic `chart_request` for `BTC-USD` using the existing frozen fixture paths `tests/fixtures/btc_usd_1d_benchmark.csv` and `tests/fixtures/polymarket_probability_history_benchmark.json`, the exact expected backend `strategy_keys`, and a `strategy_keys_with_local_buy_hold` array containing the current strategies that expose a strategy-local `buy_hold_equity_curve`. Also write `docs/current-strategy-config-ratchet.md` to explain what this ratchet protects and how to intentionally update it, and write `.planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md` as the human-readable baseline summary of the pinned defaults, strategy option order, and route strategy inventory.</action>
  <verify>python3 -m json.tool tests/fixtures/strategy_config_ratchet.json >/dev/null && rg -n '"default_strategy"|ribbon|corpus_trend|polymarket|"money_management"|"route_contract"|btc_usd_1d_benchmark.csv|polymarket_probability_history_benchmark.json' tests/fixtures/strategy_config_ratchet.json docs/current-strategy-config-ratchet.md .planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md</verify>
  <acceptance_criteria>
    - `tests/fixtures/strategy_config_ratchet.json` contains the top-level keys `backtest_defaults`, `strategy_options`, `money_management`, and `route_contract`.
    - The JSON pins `default_strategy` to `ribbon` and includes both `corpus_trend` and `polymarket` in the strategy option list.
    - The docs and baseline files both mention the frozen BTC and Polymarket fixture paths and explain that this is a configuration-contract ratchet, not a performance benchmark.
  </acceptance_criteria>
  <done>The repo has a single machine-readable config contract that later phases must intentionally update if they want to change defaults or exposed options.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic config-ratchet tests for template, JS, and route contract</name>
  <files>tests/test_strategy_config_ratchet.py, tests/fixtures/strategy_config_ratchet.json</files>
  <read_first>tests/test_focus_basket_benchmark_backtests.py, tests/test_polymarket_benchmark_backtests.py, templates/partials/backtest_panel.html, static/js/backtest_panel.js, static/js/backtest_report.js, routes/chart.py</read_first>
  <action>Create `tests/test_strategy_config_ratchet.py` that reads `tests/fixtures/strategy_config_ratchet.json` and enforces the current config contract in three places. First, request `/backtest` and assert the exact `strategy-select`, `mm-sizing`, `mm-stop`, `mm-risk-cap`, and `mm-compound` option sequences match the JSON spec. Second, assert `static/js/backtest_panel.js` still pins `BT_DEFAULT_STRATEGY` to `ribbon` and that `static/js/backtest_report.js` still falls back to `BT_DEFAULT_STRATEGY` when a requested strategy is unavailable. Third, patch `routes.chart.cached_download` with `tests/fixtures/btc_usd_1d_benchmark.csv` and patch `lib.polymarket.load_probability_history` with `tests/fixtures/polymarket_probability_history_benchmark.json`, then request `/api/chart` with the JSON's `route_contract.chart_request`. Assert the backend `strategies` keys match the spec exactly, assert every strategy has `trades`, `summary`, and `equity_curve`, assert the top-level payload has `buy_hold_equity_curve`, and assert every strategy listed in `strategy_keys_with_local_buy_hold` includes its own `buy_hold_equity_curve`.</action>
  <verify>pytest -q tests/test_strategy_config_ratchet.py</verify>
  <acceptance_criteria>
    - `tests/test_strategy_config_ratchet.py` reads `tests/fixtures/strategy_config_ratchet.json`.
    - The test file asserts exact option sequences for `strategy-select`, `mm-sizing`, `mm-stop`, `mm-risk-cap`, and `mm-compound`.
    - The test file patches both frozen BTC OHLCV and frozen Polymarket history before asserting the `/api/chart` strategy inventory and required fields.
  </acceptance_criteria>
  <done>The repo has a deterministic no-drift ratchet that catches silent changes to backtest defaults, option ordering, or route strategy mapping.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -m json.tool tests/fixtures/strategy_config_ratchet.json >/dev/null
- [ ] pytest -q tests/test_strategy_config_ratchet.py
- [ ] rg -n "default_strategy|ribbon|corpus_trend|polymarket|mm-sizing|mm-stop|mm-risk-cap|mm-compound|buy_hold_equity_curve" tests/fixtures/strategy_config_ratchet.json tests/test_strategy_config_ratchet.py docs/current-strategy-config-ratchet.md .planning/phases/03.2-lock-current-strategy-configuration-ratchet/current-strategy-config-baseline.md
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 03.2 leaves the repo with a stronger ratchet that protects the current configuration contract in addition to the existing performance baselines
  </success_criteria>

<output>
After completion, create `.planning/phases/03.2-lock-current-strategy-configuration-ratchet/03.2-01-SUMMARY.md`
</output>

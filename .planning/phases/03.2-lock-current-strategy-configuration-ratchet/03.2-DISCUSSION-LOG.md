# Phase 03.2 Discussion Log

**Date:** 2026-04-08
**Mode:** Auto-captured from current repo state and user direction

## Summary

Phase `03.2` was inserted after the user explicitly asked to strengthen the ratchet so the current backtest configurations do not regress while later strategy work continues.

## Auto-selected decisions

1. Freeze the current strategy selector and money-management controls as part of the ratchet, not just performance outputs.
2. Keep `ribbon` as the pinned default strategy until a later intentional promotion changes it.
3. Protect the route-level `strategies` contract through `/api/chart`, because that is what the backtest report actually consumes.
4. Reuse frozen BTC OHLCV and Polymarket-history fixtures so the config ratchet stays deterministic.
5. Treat this as a configuration-contract phase that hands the project back to Phase 4 once complete.

## Notes

- Existing basket and Polymarket ratchets already protect performance floors, but they do not yet freeze current option ordering, defaults, or backend strategy inventory.
- The goal here is to make accidental product drift obvious even when strategy metrics happen to stay similar.

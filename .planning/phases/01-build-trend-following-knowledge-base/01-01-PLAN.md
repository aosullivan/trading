---
phase: 01-build-trend-following-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_trend_following_kb.py
  - .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md
  - .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.json
autonomous: true
requirements:
  - KB-01
  - KB-03
user_setup: []
must_haves:
  truths:
    - Transcript files under audio/ are enumerated in deterministic filename order.
    - The KB artifact schema includes source index, principle categories, source citations, and strategy implications.
    - A validation path exists to fail on missing transcripts, duplicate source rows, and uncited principles.
  artifacts:
    - scripts/build_trend_following_kb.py
    - .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md
    - .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.json
  key_links:
    - scripts/build_trend_following_kb.py writes both KB artifacts under .planning/phases/01-build-trend-following-knowledge-base/.
    - Every source-index entry and principle citation points back to an existing audio/*.txt transcript file.
---

<objective>
Design the transcript ingestion, citation, and artifact schema for the trend-following knowledge base, and establish a deterministic generator/validator script that Phase 1 extraction can fill in.

Purpose: this creates the source-of-truth structure for turning the transcript corpus into strategy-ready knowledge without touching runtime strategy code too early.
Output: `scripts/build_trend_following_kb.py`, plus initial `trend-following-knowledge-base.md` and `trend-following-knowledge-base.json` artifacts with schema, source index, and validation hooks.
</objective>

<execution_context>
@/Users/adrianosullivan/projects/trading/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adrianosullivan/projects/trading/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-trend-following-knowledge-base/01-CONTEXT.md
@.planning/phases/01-build-trend-following-knowledge-base/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@scripts/transcribe_audiobook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define transcript inventory and KB schema contract</name>
  <files>scripts/build_trend_following_kb.py, .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md, .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.json</files>
  <read_first>.planning/phases/01-build-trend-following-knowledge-base/01-CONTEXT.md, .planning/phases/01-build-trend-following-knowledge-base/01-RESEARCH.md, .planning/REQUIREMENTS.md, scripts/transcribe_audiobook.py</read_first>
  <action>Create `scripts/build_trend_following_kb.py` with explicit constants for `AUDIO_DIR = Path("audio")`, `PHASE_DIR = Path(".planning/phases/01-build-trend-following-knowledge-base")`, `KB_MD_PATH`, and `KB_JSON_PATH`. Implement deterministic transcript enumeration via `sorted(AUDIO_DIR.glob("*.txt"))`, filename-based chapter metadata parsing, a lightweight `mode` classifier with values `credits`, `margin-quotes`, `narrative`, or `mixed`, and a structured JSON schema containing `corpus`, `principles`, and `strategy_handoff`. In the Markdown artifact, emit section headers exactly `## Corpus Coverage`, `## Source Index`, `## Principle Catalog`, and `## Strategy Design Implications`. Do not modify `lib/`, `routes/`, `templates/`, or `static/js/` in this plan because Phase 1 is artifact design only.</action>
  <verify>python3 -m py_compile scripts/build_trend_following_kb.py && rg -n "## Corpus Coverage|## Source Index|## Principle Catalog|## Strategy Design Implications" .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md</verify>
  <acceptance_criteria>
    - `scripts/build_trend_following_kb.py` contains `sorted(AUDIO_DIR.glob("*.txt"))`, `KB_MD_PATH`, `KB_JSON_PATH`, and the mode labels `margin-quotes` and `narrative`.
    - `.planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md` contains the exact section headers `## Corpus Coverage`, `## Source Index`, `## Principle Catalog`, and `## Strategy Design Implications`.
    - `.planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.json` contains top-level keys `corpus`, `principles`, and `strategy_handoff`.
  </acceptance_criteria>
  <done>The generator script and both KB artifact files exist, compile/load cleanly, and expose the schema sections and keys Phase 2 needs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement source-citation and coverage validation checks</name>
  <files>scripts/build_trend_following_kb.py, .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md, .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.json</files>
  <read_first>.planning/phases/01-build-trend-following-knowledge-base/01-RESEARCH.md, .planning/phases/01-build-trend-following-knowledge-base/01-CONTEXT.md</read_first>
  <action>Add validation routines to `scripts/build_trend_following_kb.py` that verify all `audio/*.txt` files appear exactly once in the corpus index, corpus ordering matches sorted filename order, each principle has at least one source reference, every source reference points to an existing transcript file, and every `KB-02` category is represented either by a principle or an explicit `no strong evidence found` note. Wire the script entrypoint so `python3 scripts/build_trend_following_kb.py --validate` exits non-zero on invalid coverage/citations. Keep validation deterministic and avoid introducing hidden state or network calls.</action>
  <verify>python3 scripts/build_trend_following_kb.py --validate</verify>
  <acceptance_criteria>
    - `scripts/build_trend_following_kb.py` contains checks for duplicate/missing corpus files, missing `sources`, and nonexistent source-file paths under `audio/`.
    - `python3 scripts/build_trend_following_kb.py --validate` returns success after generating the initial artifacts.
    - No files under `lib/`, `routes/`, `templates/`, or `static/js/` are modified by this plan.
  </acceptance_criteria>
  <done>The schema generator has a repeatable validation command that proves the source index and citation structure are internally consistent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 -m py_compile scripts/build_trend_following_kb.py
- [ ] python3 scripts/build_trend_following_kb.py --validate
- [ ] rg -n "## Corpus Coverage|## Source Index|## Principle Catalog|## Strategy Design Implications" .planning/phases/01-build-trend-following-knowledge-base/trend-following-knowledge-base.md
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The KB schema, transcript ordering rule, citation format, and validation command are explicit enough for `01-02` to perform corpus extraction without guessing structure
  </success_criteria>

<output>
After completion, create `.planning/phases/01-build-trend-following-knowledge-base/01-01-SUMMARY.md`
</output>

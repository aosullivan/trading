---
phase: 02-implement-corpus-derived-strategy
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lib/technical_indicators.py
  - lib/backtesting.py
  - routes/chart.py
  - templates/partials/backtest_panel.html
  - tests/test_indicators.py
  - tests/test_backtest.py
  - tests/test_routes.py
  - tests/test_ui.py
autonomous: true
requirements:
  - STRAT-02
  - STRAT-03
  - STRAT-04
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - `/api/chart` exposes a non-default `corpus_trend` strategy object with the standard `{trades, summary, equity_curve, buy_hold_equity_curve}` contract while preserving every existing strategy key.
    - The backtest UI offers a clearly named `corpus_trend` option, but `ribbon` remains first and `BT_DEFAULT_STRATEGY` remains unchanged.
    - Automated tests cover the new indicator/backtest behavior plus route/UI contract regressions and existing strategies still pass their current assertions.
  artifacts:
    - lib/technical_indicators.py
    - lib/backtesting.py
    - routes/chart.py
    - templates/partials/backtest_panel.html
    - tests/test_indicators.py
    - tests/test_backtest.py
    - tests/test_routes.py
    - tests/test_ui.py
  key_links:
    - `compute_corpus_trend_signal(...)` feeds `backtest_corpus_trend(...)` and `payload["strategies"]["corpus_trend"]` in `routes/chart.py`.
    - `templates/partials/backtest_panel.html` exposes `<option value="corpus_trend">` and `tests/test_ui.py` verifies the option exists while `ribbon` remains first/default.
    - `tests/test_routes.py`, `tests/test_backtest.py`, and `tests/test_indicators.py` verify the strategy implementation remains consistent with `.planning/phases/02-implement-corpus-derived-strategy/corpus-trend-strategy-spec.md`.
---

<objective>
Implement the `corpus_trend` strategy in backend, API, and UI, then add regression tests that protect both the new behavior and existing strategy defaults/contracts.

Purpose: this ships the first corpus-derived strategy while preserving brownfield app behavior and keeping the implementation tied to the Phase 2 spec.
Output: strategy code, `/api/chart` integration, UI option, and automated tests.
</objective>

<execution_context>
@/Users/adrianosullivan/projects/trading/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adrianosullivan/projects/trading/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-implement-corpus-derived-strategy/02-CONTEXT.md
@.planning/phases/02-implement-corpus-derived-strategy/02-RESEARCH.md
@.planning/phases/02-implement-corpus-derived-strategy/02-01-SUMMARY.md
@.planning/phases/02-implement-corpus-derived-strategy/corpus-trend-strategy-spec.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@lib/technical_indicators.py
@lib/backtesting.py
@routes/chart.py
@templates/partials/backtest_panel.html
@static/js/backtest_panel.js
@tests/test_indicators.py
@tests/test_backtest.py
@tests/test_routes.py
@tests/test_ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement corpus-trend signal, ATR-sized backtest, and chart route payload</name>
  <files>lib/technical_indicators.py, lib/backtesting.py, routes/chart.py, tests/test_indicators.py, tests/test_backtest.py, tests/test_routes.py</files>
  <read_first>.planning/phases/02-implement-corpus-derived-strategy/corpus-trend-strategy-spec.md, lib/technical_indicators.py, lib/backtesting.py, routes/chart.py, tests/test_indicators.py, tests/test_backtest.py, tests/test_routes.py</read_first>
  <action>Implement the strategy contract from `corpus-trend-strategy-spec.md` by adding a new `compute_corpus_trend_signal(...)` helper in `lib/technical_indicators.py` that uses asymmetric Donchian entry/exit lookbacks plus an ATR-based stop/risk series, and a new `backtest_corpus_trend(...)` helper in `lib/backtesting.py` that stays long/cash, sizes entries from ATR stop distance and a fixed equity risk fraction, marks open trades consistently with existing trade dicts, and returns `(trades, summary, equity_curve)`. Wire `routes/chart.py` so `_get_indicator_bundle(...)` computes and caches the corpus-trend signal outputs and `chart_data()` appends `payload[\"strategies\"][\"corpus_trend\"]` with `trades`, `summary`, `equity_curve`, and `buy_hold_equity_curve`. Preserve all existing strategy keys, `ribbon` behavior, cache-key conventions, and warmup slicing semantics; do not add macro/fundamental filters, short selling, or watchlist trend-tab expansion.</action>
  <verify>pytest tests/test_indicators.py tests/test_backtest.py tests/test_routes.py</verify>
  <acceptance_criteria>
    - `lib/technical_indicators.py` contains `compute_corpus_trend_signal` and `lib/backtesting.py` contains `backtest_corpus_trend`.
    - `routes/chart.py` contains a `corpus_trend` strategy entry and existing `ribbon` strategy payload remains present.
    - `tests/test_indicators.py`, `tests/test_backtest.py`, and `tests/test_routes.py` include assertions for `corpus_trend`, ATR-sized quantities or stop behavior, and route payload contract, and `pytest tests/test_indicators.py tests/test_backtest.py tests/test_routes.py` passes.
  </acceptance_criteria>
  <done>The new backend strategy is implemented, exposed through `/api/chart`, and covered by passing unit/route tests without breaking existing strategies.</done>
</task>

<task type="auto">
  <name>Task 2: Expose corpus-trend in the backtest UI and protect default strategy ordering</name>
  <files>templates/partials/backtest_panel.html, tests/test_ui.py, tests/test_routes.py</files>
  <read_first>.planning/phases/02-implement-corpus-derived-strategy/corpus-trend-strategy-spec.md, templates/partials/backtest_panel.html, static/js/backtest_panel.js, tests/test_ui.py, tests/test_routes.py</read_first>
  <action>Append a `<option value=\"corpus_trend\">Corpus Trend (Donchian/ATR)</option>` entry to `templates/partials/backtest_panel.html` after the existing `ribbon` option and update UI/route tests so the new strategy is selectable while the first option remains `value=\"ribbon\"` with label `Trend-Driven`. Keep `BT_DEFAULT_STRATEGY='ribbon'` unchanged in `static/js/backtest_panel.js` and avoid introducing custom frontend state for this strategy. If a lightweight test-only helper is needed, modify existing tests rather than adding new test infrastructure.</action>
  <verify>rg -n "BT_DEFAULT_STRATEGY='ribbon'" static/js/backtest_panel.js && pytest tests/test_routes.py && pytest -m ui tests/test_ui.py -k strategy_select_options</verify>
  <acceptance_criteria>
    - `templates/partials/backtest_panel.html` contains `<option value="corpus_trend">Corpus Trend (Donchian/ATR)</option>`.
    - `static/js/backtest_panel.js` still contains `BT_DEFAULT_STRATEGY='ribbon'`.
    - `tests/test_ui.py` asserts the new option exists while `ribbon` remains first, and `pytest tests/test_routes.py` plus `pytest -m ui tests/test_ui.py -k strategy_select_options` pass when the browser test environment is available.
  </acceptance_criteria>
  <done>The new strategy is visible/selectable in the backtest UI and default-strategy regression tests still protect `ribbon`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/test_indicators.py tests/test_backtest.py tests/test_routes.py
- [ ] rg -n "<option value=\"corpus_trend\">Corpus Trend \\(Donchian/ATR\\)</option>" templates/partials/backtest_panel.html
- [ ] rg -n "BT_DEFAULT_STRATEGY='ribbon'" static/js/backtest_panel.js
- [ ] pytest -m ui tests/test_ui.py -k strategy_select_options
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `corpus_trend` ships as an additive, non-default strategy with tested backend/API/UI integration and no regression to existing `ribbon` behavior or payload contracts
  </success_criteria>

<output>
After completion, create `.planning/phases/02-implement-corpus-derived-strategy/02-02-SUMMARY.md`
</output>
